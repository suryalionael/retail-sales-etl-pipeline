# Screenshots

Add generated screenshots here after running the stack:

- `airflow-dag.png`
- `great-expectations.png`
- `warehouse.png`
- `powerbi-dashboard.png`
- `demo.gif`

The README references these expected assets. They are intentionally lightweight placeholders in Git because screenshots depend on the local Docker and Power BI runtime.
